import colors from "@juel/hooks/colors";

export const lightColors = {
  main: colors.purple,
  sec: colors.blue,
  neu: colors.gray,
};
export const darkColors = {
  main: colors.orange,
  sec: colors.rose,
  neu: colors.gray,
};
